# Vite only exposes VITE_-prefixed vars, and inlines them at BUILD time.
# Changing this on Vercel requires a redeploy, not a restart. Never put a secret here.
VITE_API_BASE_URL="http://localhost:3000/api"
